{
  "$comment": [
    "Single source of truth for toolchain versions the fleet standardizes on.",
    "Nothing reads this at build time — each repo still declares its own pin in",
    "the file its tooling actually reads (package.json packageManager,",
    "package.json devDependencies, go.mod, .node-version), because that is what",
    "corepack, pnpm, setup-go and setup-node consume both in CI and on a",
    "developer's machine. This file is what those per-repo pins are checked",
    "AGAINST: tools/check-fleet-versions compares every repo to the values here,",
    "so drift is a failing check rather than something noticed months later.",
    "Bumping a version here is the start of a fleet wave, not the end: update the",
    "value, then update every repo's own pin to match."
  ],

  "$packageManager_comment": [
    "Compared against package.json's `packageManager` field, which is what",
    "corepack activates and what pnpm/action-setup reads in the shared jobs."
  ],
  "pnpm": "10.34.5",

  "$devDependencies_comment": [
    "Compared against package.json's `devDependencies`, EXACTLY -- a caret or",
    "tilde is a failure, not a near miss. A range is not a pin: two repos",
    "declaring ^7.3.0 can resolve to different vite versions on different days,",
    "which is the drift this file exists to stop. Every entry below is a package",
    "the fleet has actually converged on; a package still mid-wave does not",
    "belong here until every repo carries the value, or the check fails for",
    "repos whose turn has not come and the failure stops meaning anything.",
    "",
    "@types/node landed in wave 1 of aurum-alpha/workflows#169; the eslint six",
    "landed in wave 2; the vite four landed in wave 3; typescript landed in",
    "wave 5. All are converged across all nine Node apps -- verified against",
    "each repo's main, not assumed -- which is what makes them assertable now.",
    "",
    "React is NOT here, and its absence is not a wave still pending: wave 4 put",
    "react and react-dom on 19.2.8 in all nine repos, and this file still cannot",
    "say so. The check reads `devDependencies` only, and react/react-dom are",
    "runtime dependencies in every one of the nine -- correctly so. The types",
    "are not uniformly placed either: jewelry-factory declares @types/react in",
    "`dependencies` rather than devDependencies. So an entry added here would",
    "assert nothing about nine repos and fail on one. Asserting React needs the",
    "checker to read `dependencies` too, which is a checker change and not this",
    "file's to make. Wave 6 (typescript 7) is blocked upstream: no published",
    "typescript-eslint admits it, and 8.67.0 above peers `>=4.8.4 <6.1.0`,",
    "which is what caps the entry below at 6.0.x.",
    "",
    "The vite four are one entry in four lines, not four independent pins.",
    "@vitejs/plugin-react peers vite ^8 only from 6.0.0, and vitest reaches vite",
    "through @vitest/mocker, which admits vite 8 only from 4.1.0 -- so a repo",
    "moving vite alone gets a SECOND vite in its tree rather than a moved one,",
    "with no pnpm warning and a passing `vite build`. @vitest/coverage-v8 peers",
    "an EXACT vitest version on top of that. Bump one of these here and the",
    "other three move in the same wave, or the check passes on a tree that is",
    "quietly wrong."
  ],
  "devDependencies": {
    "@types/node": "26.3.0",
    "eslint": "10.9.1",
    "@eslint/js": "10.0.1",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.4",
    "typescript-eslint": "8.67.0",
    "typescript": "6.0.3",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "@vitest/coverage-v8": "4.1.11",
    "@vitejs/plugin-react": "6.1.1"
  }
}
