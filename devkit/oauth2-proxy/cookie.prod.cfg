# Production cookie (060 AU7 topology A, 092 TH4): __Host-<repository>_session.
# Secure, HttpOnly, SameSite=Lax, Path=/. No Domain=.
# cookie_expire is 604800s (AU5 absolute). cookie_refresh is 240s, inside the 300s token.
# The local profile drops the prefix and Secure because the stack is plain HTTP.
cookie_name = "__Host-__REPO___session"
cookie_secret = "ZGV2a2l0LWxvY2FsLW9ubHktY29va2llLXNlY3JldCE="
cookie_secure = true
